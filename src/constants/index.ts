export const CELL_SIZE = 20;
export const GRID_COLUMNS = 20;
export const GRID_ROWS = 20;

export const CANVAS_WIDTH = GRID_COLUMNS * CELL_SIZE; // = 400
export const CANVAS_HEIGHT = GRID_ROWS * CELL_SIZE;   // = 400
